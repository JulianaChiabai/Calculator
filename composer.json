{
    "name": "my-php-project/calculator",
    "description": "A simple PHP project for CI testing",
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "scripts": {
        "test": "vendor/bin/phpunit --configuration phpunit.xml"
    }
}
